package bab.com.build_a_bridge


/**
 * Helper class for dealing with calls to Firebase database.
 *
 * Rather than using String literals when calling a DB by name,
 * using enum guarantess no mistakes when calling.
 */
enum class FirebaseNames {
    USERS
}